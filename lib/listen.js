/* vim: set shiftwidth=2 tabstop=2 noexpandtab textwidth=80 wrap : */
"use strict";

export default function listen(el, ev) {
	// TODO: return a ReadableStream
	el.addEventListener(ev, ev => {
		// TODO
	});
};

